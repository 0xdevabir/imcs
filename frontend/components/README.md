# Shared Components

Use this directory only for reusable, cross-feature UI and layout primitives.

## Folders

- `ui/`: small reusable UI pieces (buttons, badges, cards, inputs, etc.)
- `layout/`: reusable layout shells (navbar, page wrappers, side rails, etc.)

Feature-specific components must live under `features/<feature-name>/components`.
